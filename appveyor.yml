version: 1.0.{build}

image: Visual Studio 2015

assembly_info:
  patch: true
  file: AssemblyInfo.*
  assembly_version: "2.2.{build}"
  assembly_file_version: "{version}"
  assembly_informational_version: "{version}"

platform: Any CPU

services:
  - mssql2014 

build:
    parallel: true
    project: \CI_TEST\CI_TEST.sln
    publish_wap: true
    verbosity: minimal


test:
  # only assemblies to test
  assemblies:
    
test_script:
    - dir 
    - cd Not-Dot-Test
    - dir
    - xunit.console "%APPVEYOR_BUILD_FOLDER%\Not-Dot-Test\bin\Debug\Not.Dot.Test.dll"  /appveyor  

  